



문자열을 숫자로 변환
parseint("123"), parseFloat("123.90"),(+"1.1")






아래값 모두 falsy value

if(false)
if(null)
if(undefined)
if(0)
if(NoN)
if('')


오브젝트는 아래와 같은 형식인데
{key:value,key2:value2,...}

1. value(값)에는 숫자, 문자, 오브젝트, 함수 등등 다들어 갈 수 있음.

2. 반드시 문자열로 처리해야 할 때가 있음 
-> 식별자규칙에 어긋날때에는 반드시 쌍따옴표를 붙여야함



이벤트는 태그에 기술하는 인라인(inline)방식과 리스너(listener)방식이 있음
-> 인라인 방식은 쓰지마삼!!








자바스크립트로 할 수 있는것

1. html 요소와 속성을 접근하여 추가, 삭제, 변경을 할 수 있음
2. html요소의 css속성을 추가, 변경, 삭제 할 수 있음
3. 이벤트 처리 및 유효성 체크할 수 있음
4. Ajax를 사용하여 json,openAPI를 활용가능

1,2 -> 이거하기엔 너무 비효율적임  -> JQuery로 대체







개발 할 때에는 uncompressed
서비스 할 때에는 compressed







id만 제외하고 반환값은 모두 배열임!
id는 유일하기때문에 0아니면 1





$('li:gt(2)') index가 2보다 큰것
$('li:not(:last)') 마지막이 아닌것 다 선택







step03_traversing.html

.eq(i) i번째 선택
.each() 매우 중요함

자바스크립트는 노드를 찾아가기가 너무힘듬
jquery는 간단하게 찾아갈 수 있고 매우 많은 traversing메서드가 존재한다.






백엔드 개발자 -> jquery만 잘하면 됨!!!!


servelet 알아야 하는이유는 jsp가 결국은 servelet으로 바뀌기때문에!!!
jsp  -> html코드에 자바코드를 삽입하면 -> servelet으로 자동으로 바뀜




servlet 그림 잘알아두기!!!! 매우 중요!















